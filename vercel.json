{
  "buildCommand": "npm install --include=dev && cd projects/booquarium && npm install --include=dev && npm run build",
  "outputDirectory": "projects/booquarium/.next",
  "installCommand": "npm install --include=dev"
}
